#include "main.h"

/**
 * add - takes two integers as input and returns their sum
 *@i: first integer parameter
 *@j: second integer parameter
 * Return: the sum of the two input integers
 */

int add(int i, int j)
{
	int sum = i + j;

	return (sum);
}
