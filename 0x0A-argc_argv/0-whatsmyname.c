#include <stdio.h>
#include "main.h"

/**
 * main - prints the its own real name at any given time
 * @argc: number of arguments supplied
 * @argv: pointer to pointer of the arguments
 * Return: 0 to indicate success
 */

int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
