#include <stdio.h>
#include <string.h>

/**
 * main - They make us do this on the first day of being
 * exposed to C? How are the absolute beginners gonna figure this out?
 * Return: Again, void
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";
	puts(str);
	return (0);
}
